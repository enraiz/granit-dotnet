# Granit.Templating.Scriban

Scriban engine implementation for Granit.Templating. Provides `ScribanTemplateEngine`
(sandboxed, multi-tenant safe), `NowGlobalContext` (`{{ now.date }}`, ...) and
`ExecutionContextGlobalContext` (`{{ context.culture }}`, ...).

Part of the [granit](https://granit-fx.dev) framework.

## Installation

```bash
dotnet add package Granit.Templating.Scriban
```

## Dependencies

- `Granit.Templating`

## Documentation

See the [full documentation](https://granit-fx.dev).
