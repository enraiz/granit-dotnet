<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>Granit.Templating.Scriban</PackageId>
    <Description>Scriban implementation of the Granit templating engine. Provides ScribanTemplateEngine (sandboxed, multi-tenant safe), NowGlobalContext ({{ now.date }}) and ExecutionContextGlobalContext ({{ context.culture }}).</Description>
    <IsPackable>true</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Granit.Templating.Scriban.Tests" />
    <InternalsVisibleTo Include="DynamicProxyGenAssembly2" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Scriban" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Granit.Templating\Granit.Templating.csproj" />
    <!-- Optional at runtime: ICurrentTenant resolved via GetService -->
  </ItemGroup>

</Project>
