<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <PackageId>Granit.Core</PackageId>
    <Description>Système de modules pour les packages Granit. Topological loading, DependsOn, lifecycle. Types domaine partagés (Entity, CreationAuditedEntity, AuditedEntity, FullAuditedEntity, ISoftDeletable).</Description>
    <IsPackable>true</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Granit.Core.Tests" />
  </ItemGroup>

  <ItemGroup>
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

</Project>
